根据Prompt.md的内容，帮我自动生成：需求文档，设计文档，单元测试文档以及用户测试文档，最后再帮我生成用户手册，分为学员手册和教练手册。

根据项目的文档，开始创建数据库，以及后端代码。

在frontend文件夹下，创建前端代码。

>> 前端页面不是登录界面
打开前端页面，并没有出现登录界面。
>> 发现没有学员和教练的测试数据。
再创建几个测试数据，包括学生，教练，以及教练创建的若干排课。
>> 学员账号显示登录失败。
张三：13800138001，区域1，密码123456这个账号显示登录失败。
>> 修复后，登录成功。
>> 学员登录后无法显示课程。
学员登录后无法显示课程。
>> 经过若干纠正后，课程显示正常。
>> next to schedule.
>> 学员点击预约后，显示预约失败。
学员点击预约后，显示预约失败。
>> 若干论调整后，失败。
>> 切换大模型到Doubao-Seed-2.0-Code后，经几轮修复后成功。
学员登录后，把可选课程和我的课程，分成两个tab页。
>> 功能变更正常。

>> 测试admin，发现admin账号登录失败。

排课管理中，如果没有学员预约的课程，操作列表不显示确认按钮。

教练排课页面的列表里，加上学员姓名，以及学员手机号，所在区域。
当前页面针对三个tab页，加一个时间筛选，下拉选项，包括：今天，明天，后天，本周，下周，本月，下月。


学员，教练和管理员三个角色的页面顶部，都加入一个登出按钮。
登出后，浏览器定位到http://localhost:5173/login，但是页面是空白的。
>> 修复成功。
在管理员界面，学员管理下增加一个“添加学员”按钮，教练管理下增加一个“添加教练”按钮。
添加学员和添加教练按钮，改成点击弹出子窗体的方式。
排课管理页面，增加一个按钮：添加排课
管理员的排课管理中，在操作列，增加一个按钮，“预约”。点开后，弹出子窗体，把系统里的学员注册到这个课程上。
预约按钮，套用橙色样式。
管理员排课管理页面，加一个“状态”筛选。